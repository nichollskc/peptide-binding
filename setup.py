#!/usr/bin/env python

from distutils.core import setup

setup(name='RationalDesign',
      version='1.0',
      author='Kath Nicholls',
      packages=['scripts', 'scripts.helper', 'scripts.models'],
      )
